@model Sistema_de_Gestion_de_Citas_Medicas.ViewModels.PacienteCreateVM

@{
    ViewData["Title"] = "Detalle del Paciente";
}

@section Styles {
    <link rel="stylesheet" href="~/css/pacientes.css" asp-append-version="true" />
}

<div class="container-fluid py-4 pacientes-page">

    
    <div class="d-flex align-items-start justify-content-between flex-wrap gap-3 mb-3">
        <div>
            <h1 class="page-title mb-1">Detalle del Paciente</h1>
            <p class="page-subtitle mb-0">Información completa del paciente</p>
        </div>

        <a asp-action="Index" class="btn btn-outline-secondary btn-pill">
            <i class="bi bi-arrow-left me-2"></i> Volver
        </a>
    </div>

    <div class="panel-card">
        <div class="panel-toolbar">
            <div class="d-flex align-items-center gap-2">
                <span class="stat-icon soft-primary">
                    <i class="bi bi-person"></i>
                </span>
                <div>
                    <div class="fw-bold">
                        @Model.Nombre @Model.Apellido
                    </div>
                    <small class="text-muted">Paciente registrado</small>
                </div>
            </div>

            <div>
                @if (Model.Activo)
                {
                    <span class="badge-pill badge-success">Activo</span>
                }
                else
                {
                    <span class="badge-pill badge-warning">Inactivo</span>
                }
            </div>
        </div>

        <div class="p-4">
            <div class="row g-4">

                <div class="col-md-6">
                    <div class="detail-item">
                        <div class="detail-label">Nombre</div>
                        <div class="detail-value">@Model.Nombre</div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="detail-item">
                        <div class="detail-label">Apellido</div>
                        <div class="detail-value">@Model.Apellido</div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="detail-item">
                        <div class="detail-label">Cédula</div>
                        <div class="detail-value">@Model.Cedula</div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="detail-item">
                        <div class="detail-label">Teléfono</div>
                        <div class="detail-value">@Model.Telefono</div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="detail-item">
                        <div class="detail-label">Fecha de Nacimiento</div>
                        <div class="detail-value">
                            @Model.FechaNacimiento?.ToString("dd/MM/yyyy")
                        </div>
                    </div>
                </div>

            </div>

            <hr class="my-4" />

            <div class="d-flex justify-content-end gap-2">
                <a asp-action="Edit" class="btn btn-warning btn-pill">
                    <i class="bi bi-pencil me-2"></i> Editar
                </a>

                <a asp-action="Index" class="btn btn-outline-secondary btn-pill">
                    Cerrar
                </a>
            </div>
        </div>
    </div>
</div>