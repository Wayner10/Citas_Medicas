@{
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
}

<nav class="navbar navbar-expand-lg navbar-modern shadow-sm">
    <div class="container-fluid">

        <a class="navbar-brand fw-bold" asp-controller="Home" asp-action="Index">
            <i class="bi bi-hospital me-2"></i>
            Sistema de Gestión de Citas Médicas
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-2">

                <li class="nav-item">
                    <a class="nav-link nav-pill @(currentController == "Pacientes" ? "active" : "")"
                       asp-controller="Pacientes" asp-action="Index">
                        <i class="bi bi-person-lines-fill me-1"></i> Pacientes
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link nav-pill @(currentController == "Doctores" ? "active" : "")"
                       asp-controller="Doctores" asp-action="Index">
                        <i class="bi bi-person-badge me-1"></i> Doctores
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link nav-pill @(currentController == "Citas" ? "active" : "")"
                       asp-controller="Citas" asp-action="Index">
                        <i class="bi bi-calendar-check me-1"></i> Citas
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link nav-pill @(currentController == "Especialidades" ? "active" : "")"
                       asp-controller="Especialidades" asp-action="Index">
                        <i class="bi bi-heart-pulse me-1"></i> Especialidades
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link nav-pill @(currentController == "Acerca" ? "active" : "")"
                       asp-controller="Acerca"
                       asp-action="Index">
                        <i class="bi bi-info-circle me-1"></i> Acerca de
                    </a>
                </li>

                

            </ul>
        </div>
    </div>
</nav>